export default async function renderStoryCard() {
    return (
        <section className="section">
            <div className="container">
                <h2>StoryCard</h2>
            </div>
        </section>
    );
}